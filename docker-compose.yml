version: '3.8'

services:

  db:
    image: postgres:15
    environment:
      POSTGRES_USER: engrenar
      POSTGRES_PASSWORD: engrenar
      POSTGRES_DB: engrenar-db
    ports:
      - "5430:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data: